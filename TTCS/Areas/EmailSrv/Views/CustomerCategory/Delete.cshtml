@model TTCS.Areas.EmailSrv.Models.ECustomerCategory

@{
    ViewBag.Title = "Delete";
}


<fieldset>
    <legend>刪除分類</legend>
    <h3>確定刪除分類 @Model.Desc (@Model.ID) ?</h3>
    @using (Html.BeginForm()) {
    <p>
        @Html.Hidden("id", (object)Model.ID)
        <input type="submit" value="刪除" /> or 
        @Html.ActionLink("取消刪除", "Index")
    </p>
    }
</fieldset>
